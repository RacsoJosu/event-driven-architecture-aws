{
  "name": "reservaciones-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "del /q dist",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outfile=dist/index.js",
    "postbuild": "cd dist && 7z a -tzip index.zip index.js*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.1",
  "devDependencies": {
    "@swc/cli": "^0.7.8",
    "@swc/core": "^1.13.5",
    "@types/aws-lambda": "^8.10.155",
    "@types/node": "^24.7.0",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.901.0"
  }
}
